"use client";

import { CustomLayout, ProfileInfo } from "@/components";

const Profile = () => {
  return (
    <CustomLayout>
      <ProfileInfo />
    </CustomLayout>
  );
};

export default Profile;
